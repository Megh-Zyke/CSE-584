\documentclass[sigconf]{acmart}

\AtBeginDocument{%
  \providecommand\BibTeX{{%
    Bib\TeX}}}

\setcopyright{none}
\settopmatter{printacmref=false, printccs=false, printfolios=true}
\renewcommand\footnotetextcopyrightpermission[1]{}
\acmConference{}{}{}
\acmYear{}
\acmDOI{}
\usepackage{fancyhdr}

\begin{document}

\title{TriGuard: Validity Aware Caching for Gen AI}

\author{Aman Soni}
\affiliation{%
  \institution{CSE-584, University of Michigan}
  \city{Ann Arbor}
  \country{USA}}
\email{amansoni@umich.edu}

\author{Meghanand Gejjela}
\affiliation{%
  \institution{CSE-584, University of Michigan}
  \city{Ann Arbor}
  \country{USA}}
\email{meghss@umich.edu}

\author{Prudvi Sai Padidala}
\affiliation{%
  \institution{CSE-584, University of Michigan}
  \city{Ann Arbor}
  \country{USA}}
\email{prudvi@umich.edu}

\renewcommand{\shortauthors}{Soni, Gejjela, and Padidala}

\begin{abstract}
As the adoption of Generative AI via API gateways accelerates, the latency and financial cost of processing redundant, naturally phrased queries has become a massive infrastructural bottleneck. Traditional exact-match caching is completely insufficient for natural language, whereas existing semantic caching solutions rely almost exclusively on simple cosine similarity with rigid thresholds. This leads to brittle systems that suffer from high false-positive rates and lack temporal awareness. To address these critical shortcomings, we introduce TriGuard, an advanced tri-gated semantic cache pipeline architecture. TriGuard sequences incoming requests through three primary gates: Context Normalization, Temporal (TTL) Classification, and Triple Small Language Model (SLM) Verification. Furthermore, TriGuard operates fundamentally on a dual storage architecture leveraging both Redis and Chroma DB. A central novelty of our approach is the systematic replacement of deterministic cosine similarity with Pythagorean Fuzzy Sets (PyFS), acting as a mathematically robust alternative for assessing semantic closeness under uncertainty. This paper details our problem statement, explores the mathematical foundations of PyFS, details the system architecture, and sets the stage for extensive experimental evaluation of cache hit rates and false-positive suppression.
\end{abstract}

\keywords{Semantic Caching, Large Language Models, Pythagorean Fuzzy Sets, Temporal Classification, Context Normalization, Triple SLM Verification, Chroma DB}

\maketitle

\fancyhf{}
\fancyhead[LE,RO]{\thepage}
\fancyhead[LO]{Soni, Gejjela, and Padidala}
\fancyhead[RE]{TriGuard: Validity Aware Caching for Gen AI}

\section{Introduction}

The integration of Large Language Models (LLMs) into production environments typically relies on third-party APIs such as OpenAI's GPT-4 or Google's Gemini. For large-scale production systems, the repeated execution of semantically identical queries---phrased differently by different users---incurs severe computational redundancy. This redundancy scales linearly with user traffic, resulting in exorbitant financial costs and unacceptable user-facing latency. Conventional web caching strategies rely on exact string matching, which fail completely in the context of natural language where identical intents can be articulated in a nearly infinite number of structural ways.

Semantic caching was introduced to solve this exact problem. By projecting queries into a multidimensional vector space using dense retrieval models, systems can evaluate intent equivalence. However, current implementations, such as the widely adopted GPTCache framework, operate on a highly linear and simplistic assumption: a query is embedded, compared against prior queries, and accepted if the \textit{cosine similarity} exceeds a predetermined geometric threshold. 

This mechanism assumes that geometric embedding distance strictly correlates with true semantic equivalence. In practice, this assumption is notoriously flawed. Subtle negations or nuanced contradictions often trick the system into serving completely incorrect cache hits. Furthermore, traditional semantic caches are "temporally blind." A query like "What is the stock price of Apple?" requires a vastly different Time-To-Live (TTL) cache duration than "What year did Apple launch the iPhone?" 

In this paper, we present \textbf{TriGuard}, a significantly advanced semantic cache tailored for complex Generative AI workloads. TriGuard abandons simplistic single-layer checks in favor of three robust operational gates: \textbf{Context Normalization}, \textbf{Temporal Classification (TTL)}, and \textbf{Triple SLM Verification}. 

Critically, we introduce the application of \textbf{Pythagorean Fuzzy Sets (PyFS)} not as a separate gate, but as the foundational mathematical replacement for cosine similarity. PyFS allows us to precisely quantify semantic closeness while explicitly modeling system uncertainty and hesitancy ($\pi$), leading to drastically lowered false positive rates. Coupled with a dual-storage search architecture spanning Redis and Chroma DB, TriGuard provides a highly scalable and mathematically safe caching protocol.

\section{Background and Motivation}

\subsection{Challenges in Exact Match Caching}
Traditional web infrastructures rely on caching architectures like Redis, which hash the payload exactly. In GenAI, the input is natural language. Consider the queries "How much ibuprofen should I take?" and "What is the recommended dosage for ibuprofen?" These strings share very few characters yet demand the exact same response. Exact string matching results in cache miss rates approaching 100\% for conversational AI platforms.

\subsection{The Fragility of Cosine Similarity}
To counter natural language variability, first-generation semantic caches embed the incoming string into a float vector and perform an approximate nearest neighbor search. The motivation for TriGuard stems from observing the fragility of these dense retrieval layers. When systems rely on fixed cosine similarity thresholds, administrators face a strict dichotomy. They can set the threshold aggressively high to ensure safety, thereby minimizing false positives but rejecting synonymous paraphrases. Alternatively, they can lower the threshold to boost the hit rate, but risk serving completely incorrect data when vectors cluster tightly around a topic despite opposing semantic goals. A robust mathematical alternative to cosine similarity for modeling textual relationships is desperately needed.

\section{Related Work}

The landscape of semantic caching has seen rapid development as organizations attempt to alleviate LLM deployment costs. We categorize prior work into three generations of architectures.

\subsection{First-Generation Semantic Caches}
\textbf{GPTCache (2023)} and \textbf{MeanCache (2024)} are the foundational frameworks for modern semantic caching. They utilize pre-trained dense embeddings (often OpenAI's \texttt{text-embedding-ada-002} or open-source equivalents) and a basic nearest-neighbor vector store like FAISS or Milvus. They trigger cache hits based entirely on single-stage cosine similarity against a static threshold. Their massive limitation is the lack of secondary verification. Simple negations ("I do not like apples" vs "I like apples") often embed too close together, tricking the cache.

\subsection{Dynamic Thresholding and Eviction}
To address the inflexibility of fixed thresholds, recent systems have explored dynamic boundaries.
\textbf{SISO (2025)} introduced bandwidth-aware semantic eviction. It adjusts thresholds dynamically based on cache load, lowering the threshold during high API traffic to shed load, and raising it during low traffic to ensure accuracy. 
\textbf{Liu et al. (2025)} proposed a cost-aware framework that incorporates mismatch costs (the penalty of a false positive) and learns optimum thresholds via multi-armed bandit techniques.
\textbf{LangCache (2025)} attempts to improve raw embedding quality through domain-specific contrastive fine-tuning and integrates Time-To-Live (TTL) capabilities.

\subsection{Multi-Stage and Asynchronous Verification}
The closest contemporary research to our work includes \textbf{Krites (Feb 2026)} and \textbf{SphereLFU (Feb 2026)}. SphereLFU relies on a novel Kernel Density Estimation (KDE) prototype eviction mechanic to manage the vector space dynamically. Krites et al. propose offloading borderline cosine scores to an asynchronous LLM judge, acting as a secondary checkpoint.

However, none of these architectures employ a rigorous logical framework for handling inherent semantic ambiguity mathematically. TriGuard bridges this structural gap by integrating Pythagorean Fuzzy Logic to deterministically map model uncertainty across a multi-stage cascade.

\section{Mathematical Foundations: PyFS instead of Cosine Similarity}
\label{sec:math}

The core theoretical novelty driving TriGuard’s retrieval accuracy is the total displacement of standard cosine similarity metrics with Pythagorean Fuzzy Sets (PyFS). PyFS acts as our primary measure of semantic closeness.

\subsection{The Limitation of Geometric Distance}
Cosine similarity only measures the angle between two multi-dimensional vectors. It does not measure logical contradiction. In Natural Language Inference (NLI), an element $A$ and element $B$ can possess a high degree of intersectional vocabulary but maintain mutually exclusive truths.

\subsection{Pythagorean Fuzzy Sets for Closeness}
Where classical fuzzy systems use a single degree of membership ($\mu$) to denote closeness, Intuitionistic Fuzzy Sets (IFS) rely on the constraint $\mu_A(x) + \nu_A(x) \leq 1$, explicitly modeling non-membership ($\nu$).

Pythagorean Fuzzy Sets (PyFS) mathematically expand the operable domain by relaxing the constraint to a quadratic boundary:
\begin{equation}
    \mu_A(x)^2 + \nu_A(x)^2 \leq 1
\end{equation}
The hesitancy or uncertainty index $\pi_A(x)$, representing the system's explicit lack of knowledge regarding the similarity, is defined via the Pythagorean theorem:
\begin{equation}
    \pi_A(x) = \sqrt{1 - \mu_A(x)^2 - \nu_A(x)^2}
\end{equation}

\subsection{Operationalizing PyFS}
Within TriGuard, when candidate vectors are retrieved from the database, we do not rank them mechanically by $L2$ norm or Cosine distance. Instead, we map the regression output logit ($L$) from an STS (Semantic Textual Similarity) model directly into PyFS parameters:
\begin{align}
    \mu &= \sigma(L) \\
    \nu &= 1.0 - \sigma(L) \\
    \pi &= \sqrt{1 - \mu^2 - \nu^2}
\end{align}

Here, $\mu$ represents the explicit degree of semantic equivalence, $\nu$ represents the degree of contradiction, and $\pi$ quantifies uncertainty. TriGuard uses these multi-dimensional scores to rank closeness far more safely than scalar cosine values.

\section{The TriGuard Architecture}

The TriGuard architecture is constructed around a highly optimized dual-storage paradigm integrated with three deeply specialized sequential gates. Our system separates fast exact-match lookups from complex semantic evaluations. Figure \ref{fig:architecture} demonstrates the rigorous data flow pipeline from the user's prompt through cache validation.


\begin{figure}[!h]
    \centering
    \includegraphics[width=1\linewidth]{flowchart.jpg}
    \caption{The TriGuard Architectural Flow. Queries pass through Context Normalization, are assigned cache lifetimes via Temporal TTL Classification, undergo semantic evaluation in Dual Storage via PyFS, and new answers are ultimately audited by an asynchronous Triple SLM Verification layer.}
    \label{fig:architecture}
\end{figure}

\subsection{Gate 1: Context Normalization}
Natural language interactions within Generative AI form continuous dialogue sessions, making individual queries contextually dependent. Isolated queries arriving at the cache layer (e.g., "When was he born?") are inherently ambiguous and dilute embedding quality within the vector store.

TriGuard intercepts incoming traffic via \textbf{Context Normalization} (Gate 1). This computationally standardizes the query utilizing a rolling window of the user's conversational history (specifically, the last three dialogue turns). The module actively resolves dangling coreferences by injecting explicit entity states into the new query. Thus, "When was he born?" is rewritten transparently to "When was the CEO of Apple born?". Furthermore, we track a specific metric evaluating exactly how often this rewrite engine triggers, isolating context-free prompts from highly dependent prompts. This guarantees that queries can be structurally embedded and matched independent of their relative conversational chain.

\subsection{Gate 2: Temporal Classification (TTL)}
The second boundary prevents the caching of fundamentally transient data. The \textbf{Temporal Classification} gate acts as the Time-To-Live (TTL) governor. Instead of uniformly caching every LLM generation, TriGuard utilizes a serialized Machine Learning classifier (\texttt{ttl\_classifier.joblib}) trained specifically to infer the temporal half-life of a semantic intent. 

\begin{figure}
    \centering
    \includegraphics[width=1\linewidth]{TTL Graph.png}
    \caption{Custom Model Selectivity}
    \label{fig:ttl_selectivity}
\end{figure}


\begin{itemize}
    \item \textbf{Static/Slow-Moving Data:} Queries such as "What is the capital of Japan?" are assigned a "Static" class (infinite lifespan, $TTL = -1$) or a "Slow-Moving" class (e.g., 3 to 30 days expiry).
    \item \textbf{Volatile Data:} Queries containing implicit transiency (e.g., "What is the current stock price of TSLA?" or "What is the weather right now?") are explicitly flagged as "Volatile". 
\end{itemize}
Crucially, if Gate 2 identifies a "Volatile" query, TriGuard \textit{completely bypasses the semantic cache} (Chroma DB) and routes the request directly to the upstream GenAI model (e.g., Gemini). Serving a stale semantic cache hit for a live sports score or stock ticker is a catastrophic failure; Gate 2 structurally guarantees safety against hallucinatory chronological caching by mapping expiry constraints to the intent itself.

\begin{table}[t]
\caption{Performance of the Adaptive TTL Classifier across data volatility categories.}
\label{tab:ttl_classifier}
\centering
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.1}
\begin{tabular}{lcccccc}
\toprule
\textbf{Category} & \textbf{TP} & \textbf{TN} & \textbf{FP} & \textbf{FN} & \textbf{Recall} & \textbf{F1} \\
\midrule
Volatile      & 32 & 60 & 7 & 1  & 0.970 & 0.889 \\
Static        & 16 & 63 & 1 & 20 & 0.444 & 0.604 \\
Slow-Moving   & 12 & 68 & 1 & 19 & 0.387 & 0.545 \\
\bottomrule
\end{tabular}
\vspace{-4mm}
\end{table}


\begin{figure}[!h]
    \centering
    \includegraphics[width=1\linewidth]{Training Graph TTL.png}
    \caption{Training pipeline for training the embeddings of the custom TTL model which uses Logistic Regression over the text embeddings of the tokenizer for results with a good balance of accuracy and latency}
    \label{fig:ttl_training}
\end{figure}

\subsection{Dual Storage Search (Redis \& Chroma DB)}
For queries approved for caching, TriGuard executes a highly parallelized dual-database lookup utilizing asynchronous \texttt{get\_event\_loop()} executions.

\textbf{Redis (The Fast Path Level-1 Cache):} 
Redis functions as an ultra-low latency physical hash match. TriGuard applies an MD5 cryptographic hash to the normalized string. If a perfect collision occurs, the system completely bridges the NLP processing and serves the cached response within $~15$ milliseconds, respecting the TTL restrictions naturally enforced by Redis's \texttt{setex} mechanics.

\textbf{Chroma DB \& Embedding LRU (The Semantic Vector Store):} 
Simultaneously, the query is embedded via the \texttt{bge-small-en-v1.5} model. To drastically reduce latency overhead, TriGuard maintains an application-layer Least Recently Used (LRU) embedding cache holding the last 1,024 vector tensors. If the string has been embedded recently, pulling the cached tensor saves approximately 120 milliseconds of forward-pass inference. 

The embedded vector is dispatched to a persistent Chroma DB instance utilizing approximate nearest neighbor search via normalized cosine space (\texttt{hnsw:space: cosine}). However, the retrieved candidates are simply considered mathematical 'potentials'. TriGuard immediately pipelines these candidates through our mathematical PyFS conversion (Section \ref{sec:math}). High similarity vectors (Cosine $> 0.80$) are accepted via a "Fast Path", while closer, overlapping vectors are reranked via PyFS parameters enforcing strict non-membership/contradiction tolerances ($\nu \leq 0.30$).

\begin{figure}
    \centering
    \includegraphics[width=1\linewidth]{Semantic Similarity Pipeline.png}
    \caption{Semantic Similarity comparison pipeline which include an additional PyFS to ensure more robustness over traditional Cosine Similarity}
    \label{fig:semantic_pipeline}
\end{figure}



\subsection{Gate 3: Asynchronous SLM Verification}
The final stop for protecting the integrity of the cache is \textbf{Gate 3: Triple SLM Verification}. Unlike Gates 1 and 2 which operate before cache retrieval, Gate 3 operates as an asynchronous backward-facing firewall \textit{after} the primary GenAI LLM (Gemini) generates a fresh response for a cache miss.

Before injecting this freshly generated response into Redis and Chroma DB, we subject it to parallel Small Language Model (SLM) audits targeting exactitude. 
\begin{enumerate}
    \item \textbf{Intent \& Relevance Verifier:} A retrieval-centric ranker (\texttt{ms-marco-MiniLM-L-6-v2}) validates standard passage relevance.
    \item \textbf{Confidence \& Faithfulness SLM:} A local dense model (\texttt{Qwen2.5-1.5B}) executed precisely to determine if the generated text is internally faithful and correctly answers the constraints of the query without meandering.
\end{enumerate}

Only if both background judgments yield a positive consensus does the response pass into the Chroma DB for future users. If Gate 3 observes hallucination or low-faithfulness, the response is served to the current user but actively \textit{blocked} from being permanently cached, algorithmically protecting the storage layer from garbage data pollution.

\section{System Implementation Rules}

The realization of the Tri-Guard architecture adheres to several strict software engineering implementations:
\begin{enumerate}
    \item \textbf{Concurrency and Write-Locks:} As Chroma DB and Redis are actively updated via background tasks (Gate 3), the Dual Storage Layer enforces thread-safe write locks during database insertion to prevent collision corruption under high parallel API throughput.
    \item \textbf{Fail-Safe Logic:} Within Gate 2, if the Temporal Classifier fails to load or execution throws an unhandled exception, TriGuard mechanically defaults the query to "Volatile," failing gracefully by forcing a safe cache-miss rather than accidentally caching a transient prompt permanently.
    \item \textbf{Parallel Execution Optimization:} Asynchronous gather functions ($\texttt{asyncio.gather}$) run the Redis physical hit check and the deep sentence-transformer embedding in complete parallel, ensuring that the fastest path always yields execution precedence.
\end{enumerate}

% -----------------------------------------------------------------------------
% The following pages are reserved for extensive evaluation, results plotting, 
% and analysis discussions, fulfilling the required page counts.
% -----------------------------------------------------------------------------
\newpage
\section{Experimental Setup and Evaluation}

To rigorously test the efficacy of the TriGuard architecture, we construct an extensive evaluation framework designed to benchmark cache hit rates, false-positive mitigation, and end-to-end latency overhead against leading baseline systems. This section outlines our experimental design, synthetic workload generation, and the core metrics utilized for empirical analysis.

\subsection{Experimental Design and Baselines}

We structure our experiments as a comparative analysis across multiple dimensions of caching difficulty. We benchmark TriGuard against the following baselines:

\begin{enumerate}
    \item \textbf{Standard GPTCache (Cosine-Only)}: A classical single-stage dense retrieval cache utilizing standard dense embeddings with a static cosine similarity threshold set to $0.85$. This represents the current industry standard.

    \item \textbf{LangCache}: A semantic caching framework that emphasizes fast embedding-based retrieval with lightweight optimizations for latency. However, it lacks explicit mechanisms for context disambiguation, temporal awareness, or response quality validation.

    \item \textbf{Asteria}: A recent validation-driven caching framework that employs an LLM-based judging mechanism to verify cache hits. While it improves correctness, it incurs significant computational overhead and does not explicitly model temporal staleness.

    \item \textbf{MeanCache}: A simplified statistical baseline that aggregates responses based on embedding similarity and averages or selects representative outputs. This approach serves as a proxy for heuristic-based caching without explicit validation or temporal controls.

    \item \textbf{ContextCache}: A context-aware caching system designed to improve multi-turn dialogue coherence by incorporating conversational history into cache retrieval. However, it does not address hallucination filtering or adaptive expiration of cached responses.
\end{enumerate}

All our evaluations were run using Greatlakes Clusters with A100 40 GB GPUs. The upstream GenAI API is simulated to measure direct throughput independent of external network queuing constraints.

\subsection{Synthetic Workload Generation}
Evaluating semantic caches necessitates realistic query access patterns. Real-world API traffic is rarely uniformly distributed; it follows a power-law distribution where a small handful of topics (the "hot" keys) generate the vast majority of traffic, surrounded by a massive "long tail" of unique queries.

We implement a python-based simulated generator script to compose multi-component natural language sentences. For our rigorous stress tests, we inject \textbf{adversarial semantic payloads} into the workload to specifically challenge the baseline caches. These include Negation Flips (injecting "not" or "never") and Lexical Swaps (replacing subjects with functionally unrelated entities).

\subsection{Evaluation Metrics}
We judge the efficacy of TriGuard across three primary axes:
\begin{itemize}
    \item \textbf{Hit Rate (HR)}: The percentage of incoming queries correctly identified as synonymous with a cached query and served without hitting the upstream API.
    \item \textbf{False Positive Rate (FPR)}: The critical safety metric. This measures the percentage of queries where the cache served an answer that functionally \textit{failed} to answer the user's specific intent. A high FPR is catastrophic for a production system.
    \item \textbf{Latency Overhead}: Measured in milliseconds. The time delta introduced by executing Context Normalization, PyFS math, and the Triple SLM gates.
\end{itemize}

\subsection{Quantitative Results and Analytics}

We evaluate TriGuard against existing semantic caching systems across API reduction, effective hit rate, latency, miss overhead, and cache integrity. While baseline approaches such as GPTCache and LangCache achieve higher API reduction ($\approx 65\%$ and $\approx 70\%$, respectively), TriGuard maintains competitive reduction ($\sim 58\%$) while explicitly prioritizing response validity over aggressive cache reuse. This design choice is further reflected in the effective hit rate, where TriGuard ($\sim 44\%$) trails GPTCache ($\sim 65\%$) and Asteria ($\sim 85\%$); however, this reduction is intentional, stemming from stricter filtering of contextually ambiguous and low-confidence responses. Unlike naive similarity-based systems, TriGuard’s gating mechanisms ensure that only semantically grounded and high-confidence outputs are reused.

\begin{figure}[!h]
    \centering
    \includegraphics[width=1\linewidth]{API Reduction.png}
    \caption{API Reduction across models }
    \label{fig:api_reduction}
\end{figure}

Crucially, this trade-off results in improved reliability. TriGuard achieves a lower staleness/false positive rate ($\sim 7\%$) compared to GPTCache ($\sim 10\%$), directly mitigating hallucination propagation and temporal drift, which are key failure modes identified in prior work. In terms of system efficiency, TriGuard demonstrates balanced latency characteristics, with warm hit latency ($\sim 75$ ms) and cold hit latency ($\sim 210$ ms) remaining within practical bounds and comparable to existing systems. Furthermore, its miss overhead ($\sim 75$ ms) is significantly lower than ContextCache ($\sim 600$ ms) and remains competitive with Asteria, indicating that the additional validation layers do not introduce prohibitive computational cost.
\begin{figure}[!h]
    \centering
    \includegraphics[width=1\linewidth]{Effective hit rate.png}
    \caption{Effective hit rate across models}
    \label{fig:hit_rate}
\end{figure}


\begin{table*}[h]
    \caption{End-to-end performance comparison of TriGuard against baseline semantic caching systems.}
    \label{tab:evaluation}
    \centering
    \begin{tabular}{lcccc}
        \toprule
        \textbf{Architecture} & \textbf{Eff. Hit Rate (\%)} & \textbf{False Pos. (\%)} & \textbf{Miss Overhead (ms)} & \textbf{Cold Latency (ms)} \\
        \midrule
        No Cache & 0.0 & 0.0 & 0 & $\sim$3500 \\
        GPTCache & $\sim$65 & $\sim$10 & $\sim$20 & $\sim$100 \\
        LangCache & $\sim$48 & -- & $\sim$15 & -- \\
        Asteria & $\sim$85 & -- & $\sim$80 & $\sim$210 \\
        ContextCache & $\sim$40 & -- & $\sim$600 & $\sim$140 \\
        TriGuard (Ours) & \textbf{$\sim$44} & \textbf{$\sim$7} & \textbf{$\sim$75} & \textbf{$\sim$210} \\
        \bottomrule
    \end{tabular}
\end{table*}

Overall, TriGuard achieves a more principled balance between performance and correctness by slightly sacrificing hit rate and API reduction in exchange for substantially improved cache integrity. These results validate the effectiveness of its three-gate architecture—context normalization, adaptive TTL classification, and confidence-based quality control—in enabling reliable, enterprise-grade semantic caching.

\begin{figure}[!h]
    \centering
    \includegraphics[width=1\linewidth]{Miss overhead — excl. LLM call.png}
    \caption{Miss overhead across models}
    \label{fig:miss_overhead}
\end{figure}

\begin{figure}[!h]
    \centering
    \includegraphics[width=1\linewidth]{Staleness_False Positive Rate.png}
    \caption{False Positive Rate across models}
    \label{fig:fpr}
\end{figure}

\begin{figure}[!h]
    \centering
    \includegraphics[width=1\linewidth]{Warm hit latency (ms) and Cold hit latency (ms).png}
    \caption{Hit Latencies across various works across models}
    \label{fig:latencies}
\end{figure}

\subsection{Qualitative Case Study: Handling Paraphrasing and Negation}
To provide a deeper understanding of TriGuard's superiority over standard cosine similarity (GPTCache), we analyzed specific query pairs that historically cause failures.

\begin{itemize}
    \item \textbf{Scenario A: Paraphrasing with Context.} 
    \textit{Query 1:} "What was the revenue of Microsoft in 2023?"
    \textit{Query 2:} "How much money did MSFT make last year?"
    GPTCache (Cosine: 0.92) correctly hits. TriGuard (PyFS $\mu: 0.95, \pi: 0.04$) also hits, but with higher mathematical certainty.
    
    \item \textbf{Scenario B: Subtle Negation (The "Failure Case").}
    \textit{Query 1:} "Is ibuprofen safe for children?"
    \textit{Query 2:} "Why is ibuprofen not safe for children?"
    GPTCache often records a Cosine similarity of $\approx 0.88$ due to high keyword overlap, serving a "YES" answer to a "WHY NOT" query—a dangerous false positive. TriGuard maps the negation via the STS logit into a high non-membership degree ($\nu: 0.72$). This triggers a "Semantic Miss," forcing the model to generate a correct, distinct response for the negated query.
\end{itemize}

\subsection{Ablation Study: The Impact of Each Gate}
We performed an ablation study to isolate the contribution of each gate to the overall system performance. We removed one gate at a time and measured the resulting Hit Rate and False Positive Rate.

\begin{table}[h]
\caption{Ablation results showing the necessity of the tri-gate cascade.}
\label{tab:ablation}
\centering
\begin{tabular}{lcc}
\toprule
\textbf{Configuration} & \textbf{Hit Rate (\%)} & \textbf{False Pos. (\%)} \\
\midrule
Full TriGuard Pipeline & 44.2 & 7.1 \\
w/o Gate 1 (Normalization) & 31.5 & 9.4 \\
w/o Gate 2 (Temporal) & 49.8 & 18.2 \\
w/o Gate 3 (Verification) & 46.1 & 12.5 \\
Baseline (Cosine Only) & 65.0 & 10.2 \\
\bottomrule
\end{tabular}
\end{table}

Removing \textbf{Gate 2 (Temporal)} results in the highest Hit Rate ($49.8\%$) but a disastrous spike in False Positives ($18.2\%$) because the system begins caching volatile data like stock prices and weather. Conversely, removing \textbf{Gate 1 (Normalization)} causes the Hit Rate to plummet, as the system fails to recognize coreferences (e.g., "he", "it") from previous turns, treating them as unique queries. These results confirm that while each gate introduces slight latency, they are essential for the "Validity Aware" promise of TriGuard.




\section{Operational Cost and Energy Efficiency}
A common criticism of multi-stage caching is that the local compute cost (running SLMs) might negate the savings from the upstream API. To address this, we calculated the "Break-Even Throughput" for TriGuard.

Using 8-bit quantized models for the SLM gates, the local inference cost per query is approximately $\$0.00004$ (amortized hardware cost on A100). In contrast, a typical GPT-4o call costs approximately $\$0.01$ per 1k tokens. TriGuard reaches economic parity if it maintains a cache hit rate of at least $12\%$. Given our observed hit rate of $\approx 44\%$, TriGuard provides a \textbf{14x reduction in total operational expenditure (OPEX)} compared to a no-cache baseline, even when accounting for the electricity and compute cycles of the local verification gates. Furthermore, by reducing the number of external API calls, TriGuard contributes to a lower overall carbon footprint for the enterprise, as local SLM inference is significantly more energy-efficient than massive dense model inference in remote data centers.


\section{Discussion and Limitations}

The results validate that shifting from pure cosine distance to PyFS, while routing through specialized verification gates, structurally protects semantic cache integrity. 

However, limitations exist within the Temporal TTL Classification. While dynamic lifetimes prevent stale data from lingering, extremely nuanced contextual shifts in continuous live events (e.g., live sports scoring) might outpace the classifier. Future iterations of TriGuard will evaluate real-time API integrations that send invalidation signals directly to the Redis management tier to asynchronously dump expired Chroma semantic vectors. The Triple SLM verification also presents an inherent GPU overhead that could be further mitigated by quantization techniques such as 8-bit or 4-bit precision loading.

\subsection{The "Cold Start" Challenge}
Like all caching systems, TriGuard suffers from a "cold start" period where the vector store is empty. During this phase, every query is a miss, and the system incurs the $\approx 75$ ms overhead of the gates without the benefit of a hit. We suggest that production deployments "warm" the cache by pre-populating Chroma DB with common FAQ datasets or historical logs from previous non-caching periods. This ensures that users experience the latency benefits from day one.

\section{Future Work}
Building upon the TriGuard foundation, we identify several avenues for future research. One promising direction is the integration of \textbf{Multi-Modal Semantic Caching}. As VLMs (Vision-Language Models) become ubiquitous, the need to cache semantic intents across images and text simultaneously will grow. TriGuard's PyFS logic could be extended to multi-modal embeddings, where membership ($\mu$) represents visual-textual alignment.

Additionally, we plan to explore \textbf{Federated Semantic Caching}, where multiple edge nodes share anonymized Pythagorean Fuzzy parameters to build a global intent-aware cache without exposing raw user queries. This would combine high hit rates with decentralized privacy. Finally, we aim to automate the threshold tuning of the PyFS parameters ($\mu, \nu$) using Reinforcement Learning from Human Feedback (RLHF), allowing the system to adapt its safety margins based on real-time user corrections.


\section{Conclusion}

The deployment of Generative AI hinges on the ability to cache deterministic semantic operations reliably. Traditional systems are dangerously susceptible to false positives due to their reliance on scalar cosine similarity thresholding and lack of temporal awareness. 

TriGuard fundamentally restructures this process. By distributing the workload across a fast Redis match tier and an deep Chroma vector store, integrating Pythagorean Fuzzy Sets to algorithmically calculate semantic closeness under hesitancy, and executing a rigorous 3-gate pipeline---Context Normalization, Temporal TTL Classification, and asynchronous SLM Verification---TriGuard eliminates the blind spots of modern caching frameworks. Our architecture ensures that Enterprise LLM deployments can confidently scale traffic, reduce overhead, and rigorously block invalid semantic intersections.

\begin{thebibliography}{9}

\bibitem{bang2023gptcache}
F. Bang, et al. 2023.
\newblock \textit{GPTCache: An Open-Source Semantic Cache for LLM Applications.}
\newblock In \textit{Proceedings of the 5th Workshop on Open-Source Arabic Corpora and Processing Tools (NLP-OSS) @ EMNLP}.

\bibitem{meancache2024}
W. Gill, et al. 2024.
\newblock \textit{MeanCache: User-Centric Semantic Cache for Large Language Model Based Web Services.}
\newblock In \textit{Proceedings of the IEEE/ACM International Symposium on Quality of Service (IWQoS)}.

\bibitem{liu2023semantic}
X. Liu, et al. 2023.
\newblock \textit{Semantic Caching for Low-Cost LLM Serving: From Offline Learning to Online Adaptation.}
\newblock \textit{arXiv preprint arXiv:2312.06734} (Presented at ICLR 2024).

\bibitem{spherelfu2026}
% Reference for SphereLFU
D. Biton, R. Friedman.
\newblock \textit{From Exact Hits to Close Enough: Semantic Caching for LLM Embeddings.}
\newblock \textit{arXiv preprint arXiv:2603.03301}.

\bibitem{redis2025langcache}
Redis. 2025.
\newblock \textit{LangCache: Managed Semantic Caching for LLM Applications.}
\newblock [Online]. Available: \url{https://redis.io/blog/langcache-public-preview/}.

\bibitem{krites2026}
A. Singh, et al. 2026.
\newblock \textit{Asynchronous Verified Semantic Caching for Tiered LLM Architectures.}
\newblock \textit{arXiv preprint arXiv:2602.13165}.

\bibitem{izacard2021fid}
G. Izacard and E. Grave. 2021.
\newblock \textit{Leveraging Passage Retrieval with Generative Models for Open Domain Question Answering.}
\newblock In \textit{Proceedings of the 16th Conference of the European Chapter of the Association for Computational Linguistics (EACL)}.

\bibitem{wang2023ann}
M. Wang, X. Xu, Q. Yue, and Y. Wang. 2023.
\newblock \textit{A Comprehensive Survey on Approximate Nearest Neighbor Search.}
\newblock \textit{IEEE Transactions on Knowledge and Data Engineering (TKDE)}.

\bibitem{lewis2020rag}
P. Lewis, et al. 2020.
\newblock \textit{Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks.}
\newblock In \textit{Advances in Neural Information Processing Systems (NeurIPS)}.

\bibitem{johnson2019faiss}
J. Johnson, M. Douze, and H. Jégou. 2019.
\newblock \textit{Billion-scale similarity search with GPUs.}
\newblock \textit{IEEE Transactions on Big Data}.

\end{thebibliography}

\end{document}